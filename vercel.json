{
  "version": 2,
  "buildCommand": "npm install -g npm@10.9.2 && npm config set maxsockets 3 && cd server && npm install --no-audit --no-fund && npx prisma generate && npx tsx prisma/seed.ts && cd ../client && npm install --no-audit --no-fund && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "server/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}

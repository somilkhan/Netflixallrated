{
  "version": 2,
  "buildCommand": "npm config set maxsockets 3 && cd server && npm install --no-audit --no-fund && npx prisma generate && sh scripts/migrate.sh && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "includeFiles": "server/**"
    }
  },
  "crons": [
    {
      "path": "/api/titles/sync-batch",
      "schedule": "0 2 * * *"
    }
  ],
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}

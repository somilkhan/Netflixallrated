{
  "version": 2,
  "installCommand": "echo 'skipping root install — buildCommand installs server/client separately'",
  "buildCommand": "cd server && yarn install && npx prisma generate && sh scripts/migrate.sh && cd ../client && yarn install && yarn build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "includeFiles": "server/**"
    }
  },
  "crons": [
    {
      "path": "/api/titles/sync-batch",
      "schedule": "0 2 * * *"
    }
  ],
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}

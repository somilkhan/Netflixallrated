{
  "version": 2,
  "buildCommand": "npm install -g npm@10.9.2 && cd server && npm config set fetch-retries 5 && npm install --no-audit --no-fund && npx prisma generate && cd ../client && npm config set fetch-retries 5 && npm install --no-audit --no-fund && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "server/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
